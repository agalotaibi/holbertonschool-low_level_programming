#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
char alph = 'a';
while (alph <= 'z')
{
putchar(alph);
alph ++;
}
char capAleph = 'A';
while (capAleph <= 'Z')
{
putchar(capAleph);
capAleph ++;
}
putchar('\n');
return (0);

}
