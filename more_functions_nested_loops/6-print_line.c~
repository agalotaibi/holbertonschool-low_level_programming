#include "main.h"

/**
* more_numbers - Prints the numbers 0-14 ten times.
*/

void print_line(int n)
{
int length;

if (n > 0)
{
for (length = 0; length < n; length++)
{
_putchar('_');
}
}
_putchar('\n');

}
